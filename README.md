# OBD11 VAG Bypass iOS 14

For **VAG 1.9.28** on **rootful iOS 14**. Keeps the update bypass and version headers.

**1.0.6 is a login diagnostic build, not a confirmed login fix.** It applies the headers directly to Parse requests and adds connection/server error codes to the login popup. Close and reopen VAG, try logging in, and send a screenshot if it fails. No credentials or tokens are logged.
